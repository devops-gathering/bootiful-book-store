spring:
  datasource:
    url: "jdbc:postgresql://localhost/postgres"
    username: "postgres"
    password: "postgres"

spring.jpa:
  database-platform: org.hibernate.dialect.PostgreSQLDialect
  generate-ddl: true
  show-sql: true
  hibernate:
    ddl-auto: create